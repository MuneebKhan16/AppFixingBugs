import { Text, StyleSheet, View } from 'react-native'


export const styles = StyleSheet.create({
    maincontainer: {
        flex: 1,
        alignItems: 'center',
        width: '100%',
    },
    container:{
        alignItems: 'center',
        flex: 1,
        width: '100%',
      },
   
})